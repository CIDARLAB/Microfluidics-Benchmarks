// Same gradient-style numeric mixing as gradient.lfr; avoid nested fluid−fluid `-`
// inside one bracket expression (parser/eval order).
module issue8(
    finput p, q, r,
    foutput [0:8] out
);
    assign out[0] = p;
    assign out[1] = (1.0/9.0)*(6.0*p + 3.0*q);
    assign out[2] = (1.0/36.0)*(15.0*p + 18.0*q + r);
endmodule
