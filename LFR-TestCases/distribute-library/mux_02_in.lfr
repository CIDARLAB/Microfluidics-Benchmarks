module mux_02_in(
    finput [0:1] inputs, 
    foutput output, 
    control c_select
);

    distribute@(c_select)
    begin
        if(c_select == 1)
            output <= inputs[0];
        else
            output <= inputs[1];
    end

endmodule
