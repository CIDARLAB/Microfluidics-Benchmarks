I want to have a device called ''import_droplet_reaction'' with four fluid inputs named analyte, primer, buffer, and oil_phase, and one fluid output named outlet. Build this device by importing and instantiating reusable library modules only — do not redefine those modules or invent their internal logic.

Import these library modules (use these exact module names and port names):
- three_in_mixer from "library/three_in_mixer.lfr": fluid inputs in_a, in_b, in_c; fluid output out_1.
- droplet_generator from "library/droplet_generator.lfr": fluid inputs oil, aqueous; fluid output droplets.
- incubator from "library/incubator.lfr": fluid input in_1; fluid output out_1.

LFR import writing for this device (do not copy the English word "from" into LFR): each import line is one leading backtick, then import, then a quoted path only, e.g. `import "library/three_in_mixer.lfr"
Forbidden: import Name from "...";  `import Name from "...";  or import "..." without the leading backtick. Put the module name only on the instantiation with named port maps, e.g. three_in_mixer u0(.in_a(net1), .in_b(net2), .in_c(net3), .out_1(mid));

Wiring: connect analyte, primer, and buffer to three_in_mixer.in_a, in_b, and in_c; take three_in_mixer.out_1 as the aqueous stream; connect that aqueous stream to droplet_generator.aqueous and oil_phase to droplet_generator.oil; connect droplet_generator.droplets to incubator.in_1; connect incubator.out_1 to outlet. Write LFR code for this device.
