I want to have a device called ''dx2'' with two fluid inputs named in1 and in2, and two fluid outputs named out1 and out2. Liquid from in1 is metered into droplets with a nominal volume of 100, and liquid from in2 is metered into droplets with a nominal volume of 200. The two droplet streams then merge. The merged droplets pass through a mixer (tech-mapped from the unary operator "~" to a MIXER) for incubation. Finally, the incubated mixture is split so that out1 carries the kept stream and out2 is a discard or side-branch outlet (out1 equals the incubated mixture minus out2). Write LFR code for this device.
