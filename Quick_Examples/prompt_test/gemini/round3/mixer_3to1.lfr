module mixer_3to1(
    finput in1, in2, in3,
    foutput out
);

flow intermediate;

// Stage 1: merge in1 and in2 into an intermediate stream
assign intermediate = in1 + in2;

// Stage 2: merge intermediate stream with in3
assign out = intermediate + in3;

endmodule