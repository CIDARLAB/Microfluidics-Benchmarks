module mixer_3to1(
    finput in1, in2, in3,
    foutput out
);
flow intermediate, final;

// Stage 1: merge in1 and in2
assign intermediate = in1 + in2;

// Stage 2: merge intermediate with in3
assign final = intermediate + in3;

// Output final mixture
assign out = final;

endmodule