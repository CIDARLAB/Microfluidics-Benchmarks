module flow_and_control_demo(
    finput inlet1, inlet2, inlet3,
    control c,
    foutput outlet
);

flow mixer_stream;

assign mixer_stream = inlet1 + inlet2;

distribute@(c)
begin
    if (c == 1'b1) outlet <= mixer_stream;
    else outlet <= inlet3;
end

endmodule