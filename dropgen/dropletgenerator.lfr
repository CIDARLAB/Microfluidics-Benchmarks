module dropletgenerator(waterin, dropletout);

    finput waterin;
    foutput dropletout;

    #MAP 'DROPLET GENERATOR' '&'
    #CONSTRAIN '&' DROPLET_RATE = 30 Hz AND DROPLET_SIZE = 85 um
    assign dropletout = &waterin ;

endmodule
