module mixer_3to1(
    finput in1, in2, in3,
    foutput out
);

flow mixed;

// Stage 1: merge in1 with in2
assign mixed = in1 + in2;

// Stage 2: merge intermediate stream with in3
assign out = mixed + in3;

endmodule