module mixer_3to1(
    finput in1, in2, in3,
    foutput out
);
flow mid, final_mix;

#MAP "MIXER" "~"
assign mid = ~(in1 + in2);

#MAP "MIXER" "~"
assign final_mix = ~(mid + in3);

assign out = final_mix;
endmodule