module flow_only_demo(
    finput inlet_1, inlet_2,
    foutput outlet_1
);

flow mixed;

// Mix the two input liquids.
assign mixed = inlet_1 + inlet_2;

// Send the mixed liquid to the output.
assign outlet_1 = mixed;

endmodule