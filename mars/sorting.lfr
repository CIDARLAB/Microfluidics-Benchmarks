module sorting (
    finput A,
    foutput a_out, b_out, c_out
);
    flow temp;
    // sorter
    assign a_out = A - temp;
    assign temp = {b_out, c_out};

    // assign b_out = ~A;
    // assign c_out = ~A;

endmodule