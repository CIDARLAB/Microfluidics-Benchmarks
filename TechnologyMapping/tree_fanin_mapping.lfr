module tree_fanin(i1[0:7], out);

    finput [0:7] i1;
    foutput out;

    // This implicitly means that 
    // we can generate a tree
    #MAP 'TREE' 'assign'
    assign out = i1 [0:7];

endmodule

